# ADR 0003: Privacy-Conscious Defaults

TaskSignal stores `author_hash` instead of raw usernames by default. It keeps source URLs for attribution and avoids collecting unnecessary personal data.

The tool is intended for research and product discovery, not spam or harassment.

